# Core web framework
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
pydantic==2.7.0
pydantic-settings==2.3.0

# LLM providers (user picks one via LLM_PROVIDER env)
anthropic==0.39.0
openai==1.51.0

# HTTP / utilities
httpx==0.27.0
python-dotenv==1.0.1
cryptography==42.0.8
aiofiles==23.2.1

# Web grounding for the AgenticLoop (search + fetch + readability).
# Multiple no-API-key backends are tried in a fallback chain inside
# app/tools/web.py - DuckDuckGo, Google (direct scrape), SearXNG public
# instances, and Wikipedia. None of these require the user to register
# anywhere or paste a key.
duckduckgo-search==6.3.0
googlesearch-python==1.2.4
trafilatura==1.12.2

# Data
pandas==2.2.2
numpy==1.26.4
pypdf==4.2.0
python-docx==1.1.0
beautifulsoup4==4.12.3
python-magic==0.4.27

# Training stack (only needed if running real jobs locally)
torch==2.3.0
transformers==4.41.0
peft==0.11.0
bitsandbytes==0.43.1
accelerate==0.30.0
datasets==2.19.0
sentencepiece==0.2.0
huggingface-hub==0.23.0
trl==0.9.4
galore-torch==1.0

# Dev
pytest==8.2.0
pytest-asyncio==0.23.6
